[project]
name = "nine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Guruprasad AH", email = "guruprasadah08@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.13.4",
]

[project.scripts]
nine = "nine:main"

[build-system]
requires = ["uv_build>=0.10.9,<0.11.0"]
build-backend = "uv_build"
